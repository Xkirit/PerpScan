# Bybit API Credentials
# Get your API key from: https://www.bybit.com/app/user/api-management
# Only "Read" permissions are needed (no trading permissions required)

BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here

# Instructions:
# 1. Copy this file to .env.local
# 2. Replace the placeholder values with your actual Bybit API credentials
# 3. Make sure .env.local is in your .gitignore (it should be by default)
# 4. For Vercel deployment, add these as environment variables in your Vercel dashboard 